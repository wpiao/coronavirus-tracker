// Create config.js file in the same folder and copy this code to your config.js file.
// Replace user and password info with your own mysql credientials.

const config = {
  host: 'localhost',
  user: 'your_username', // replace me
  password: 'your password', // replace me
  database: 'coronavirus_tracker'
};

module.exports = { config };